mkdir : make or create directory
cd : change directory 
ls : list the itemsin the directory [ls -r, ls -ls, ls -a, ls -al]
history : to spill out all previously used command [history -d <line number>, history -c <to clear all history>]
pwd : [print working/current directory]
useradd and usermod : [Add new user or change existing users data]
passwd : [Create or update passwords for existing use]
cat: [create a file, display content of the file, copy the content of one file to another file]
clear: [to clear stuff and have a fresh terminal]

{
git init
git config user.name "name"
git config user.email "email"
}


{Pushing code to GitHub from Terminal}

git clone <link>
git status
git add *
git commit -m "message" (ctrl x + ctrl c & :wq to exit editor if -m not given)
git push

{Pushing through Branch}

git branch <branch name>
git checkout_<branch name> [moves to the new branch we created]
git push -f -origin -test